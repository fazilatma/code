import test from 'node:test';import assert from 'node:assert/strict';import {readFile,rm} from 'node:fs/promises';import {build} from 'esbuild';
let modulePromise;
async function scraper(){if(!modulePromise)modulePromise=(async()=>{await build({entryPoints:['render-src/scraper.ts'],outfile:'.scraper-test.mjs',bundle:true,platform:'node',format:'esm',target:'node20',packages:'external'});return import(`../.scraper-test.mjs?${Date.now()}`)})();return modulePromise}
test.after(async()=>rm('.scraper-test.mjs',{force:true}));
test('product parser matches expected PHP-style selectors and Persian prices',async()=>{const m=await scraper(),html=await readFile('render-tests/fixtures/shop.html','utf8'),products=m.parseProductHtml(html,'https://shop.example/category/perfume',{container:'li.product',title:'h2.title',price:'.price',link:'a.product-link',image:'img'});assert.equal(products.length,2);assert.equal(products[0].title,'ادو پرفیوم زنانه');assert.equal(products[0].price,1250000);assert.equal(products[0].url,'https://shop.example/p/perfume-1');assert.equal(products[0].image,'https://shop.example/images/one.jpg');assert.equal(products[1].price,850000)});
test('product parser produces stable source keys',async()=>{const m=await scraper(),html=await readFile('render-tests/fixtures/shop.html','utf8'),selectors={container:'li.product',title:'h2.title',price:'.price',link:'a.product-link',image:'img'},a=m.parseProductHtml(html,'https://shop.example/',selectors),b=m.parseProductHtml(html,'https://shop.example/',selectors);assert.deepEqual(a.map(x=>x.sourceKey),b.map(x=>x.sourceKey))});
