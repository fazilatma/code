import test from 'node:test';import assert from 'node:assert/strict';import {readFile,rm} from 'node:fs/promises';import {build} from 'esbuild';
let promise;async function lib(){if(!promise)promise=(async()=>{await build({entryPoints:['render-src/media.ts'],outfile:'.media-test.mjs',bundle:true,platform:'node',format:'esm',target:'node20',packages:'external'});return import(`../.media-test.mjs?${Date.now()}`)})();return promise}test.after(()=>rm('.media-test.mjs',{force:true}));
test('media discovery combines OpenGraph, gallery, srcset and JSON-LD images',async()=>{const m=await lib(),html=await readFile('render-tests/fixtures/media.html','utf8'),images=m.parseDiscoveredImages(html,'https://shop.example/product/1');for(const expected of ['https://shop.example/images/main.jpg','https://shop.example/images/large.jpg','https://shop.example/large-2.jpg','https://cdn.example/gallery-1.webp','https://shop.example/images/gallery-2.png'])assert.ok(images.includes(expected),expected);assert.equal(new Set(images).size,images.length)});
