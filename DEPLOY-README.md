# deploy.php — انتقال خودکار اسکریپر از گیت‌هاب به هاست

دیگر نیازی به کپی/پیست دستی نیست. بعد از هر تغییری که من روی گیت‌هاب push می‌کنم،
یک آدرس را در مرورگر باز می‌کنید و فایل روی هاست به‌روز می‌شود.

---

## نصب (فقط یک‌بار)

**۱.** فایل `deploy.php` را در همان پوشه‌ای که `scraper.php` قرار دارد آپلود کنید.

**۲.** فایل را باز کنید و مقدار `token` را به یک رشتهٔ تصادفی و طولانی تغییر دهید:

```php
'token' => 'یک-رشتهٔ-تصادفی-طولانی-اینجا',
```

برای ساخت یک توکن خوب می‌توانید از این استفاده کنید:
`https://www.random.org/strings/?num=1&len=32&digits=on&upperalpha=on&loweralpha=on&format=plain`

**۳.** تمام. آماده است.

---

## استفادهٔ روزمره

| کار | آدرس |
|---|---|
| **به‌روزرسانی** | `https://example.com/deploy.php?token=TOKEN` |
| **فقط بررسی** (بدون تغییر) | `https://example.com/deploy.php?token=TOKEN&check=1` |
| **بازگشت به نسخهٔ قبل** | `https://example.com/deploy.php?token=TOKEN&rollback=1` |

خروجی نمونهٔ یک به‌روزرسانی موفق:

```
→ scraper-v8.17.php
  حجم    : 777,281 بایت
  syntax : سالم
  sha256 : cee15d3e5445
  بکاپ   : scraper.php.20260801-033112.bak
  ✓ نصب شد → scraper.php

== نتیجه ==
به‌روزرسانی: 1   بدون تغییر: 0   ناموفق: 0
```

اگر چیزی تغییر نکرده باشد، پیام `= بدون تغییر` می‌بینید و فایل دست نمی‌خورد.

---

## چه چیزی شما را محافظت می‌کند

فایل روی هاست **فقط وقتی** جایگزین می‌شود که همهٔ این شرط‌ها برقرار باشند:

1. دانلود با موفقیت و کد ۲۰۰ انجام شده باشد
2. حجم فایل از حد حداقل بیشتر باشد (جلوی دانلود ناقص را می‌گیرد)
3. فایل با `<?php` شروع شود (جلوی ذخیرهٔ صفحهٔ خطای HTML را می‌گیرد)
4. **فایل از نظر نحوی سالم باشد** — با `token_get_all()` بررسی می‌شود
5. بکاپ‌گیری از نسخهٔ فعلی موفق باشد
6. فایل موقت نوشته و هش آن تأیید شود، سپس **اتمیک** جایگزین شود

اگر هر کدام از این‌ها شکست بخورد، عملیات لغو می‌شود و **سایت شما دست‌نخورده باقی می‌ماند**.
این سناریوها واقعاً تست شده‌اند: فایل خراب، فایل ناقص، صفحهٔ HTML به‌جای PHP، و خطای ۴۰۴ —
در هر چهار مورد فایل سالم روی هاست آسیب ندید.

بکاپ‌ها در `_backups/` ذخیره می‌شوند (۱۰ نسخهٔ آخر) و با `.htaccess` از دسترسی وب محافظت شده‌اند.

---

## تنظیمات

```php
'branch' => 'arena/019fb317-code',   // برنچی که از آن می‌خوانید

'files' => [
    'scraper-v8.17.php' => 'scraper.php',   // مبدأ در گیت‌هاب => مقصد روی هاست
],
```

می‌توانید چند فایل را هم‌زمان مدیریت کنید:

```php
'files' => [
    'scraper-v8.17.php' => 'scraper.php',
    'gold-api.php'      => 'gold-api.php',
],
```

**ریپوی خصوصی؟** یک [Personal Access Token](https://github.com/settings/tokens) با دسترسی `repo`
بسازید و در `github_token` بگذارید. (ریپوی فعلی عمومی است و نیازی نیست.)

---

## نکات امنیتی

- توکن را در آدرس مرورگر وارد می‌کنید، پس ممکن است در لاگ سرور ثبت شود. اگر حساس است،
  `deploy.php` را با نام غیرقابل‌حدس (مثلاً `deploy-8f3k2.php`) آپلود کنید.
- بعد از اتمام کار، اگر مدتی نیازش ندارید می‌توانید فایل را موقتاً حذف کنید.
- **هرگز** توکن گیت‌هاب را داخل فایلی که در ریپوی عمومی commit می‌شود نگذارید.

---

## اجرای خودکار (اختیاری)

اگر می‌خواهید بدون باز کردن مرورگر انجام شود، در cPanel یک cron job بسازید:

```
*/30 * * * * /usr/bin/php /home/USER/public_html/deploy.php >/dev/null 2>&1
```

در حالت خط فرمان توکن لازم نیست (چون از طریق وب قابل دسترسی نیست).
