# وقتی هاست فایل را حذف می‌کند

اسکنرهای امنیتی هاست‌های اشتراکی (ImunifyAV، ClamAV، Patchman و مشابه)
با «امضا» و «رفتار» کار می‌کنند، نه با فهمیدن کد. یک فایل PHP بزرگ که
هم درخواست شبکه می‌فرستد و هم فایل می‌نویسد، از نظر آماری شبیه
شل (web shell) است — حتی اگر هیچ کار مخربی نکند.

## چه چیزی در این فایل نیست

اسکنرها معمولاً دنبال این‌ها می‌گردند. هیچ‌کدام در این کد وجود ندارد:

| الگو | وضعیت |
|---|---|
| `eval(` | ❌ صفر مورد |
| `base64_decode` | ❌ صفر مورد |
| `gzinflate` / `gzuncompress` / `str_rot13` | ❌ صفر مورد |
| `shell_exec` / `system` / `passthru` / `exec` | ❌ صفر مورد |
| `proc_open` / `popen` | ❌ صفر مورد |
| `assert(` / `create_function` | ❌ صفر مورد |
| `preg_replace` با پرچم `/e` | ❌ صفر مورد |
| `$_REQUEST` / `php://input` | ❌ صفر مورد |
| متغیر متغیر (`$$x`) | ❌ صفر مورد |
| رشتهٔ بلند base64 | ❌ صفر مورد |
| بازنویسی خود فایل | ❌ صفر مورد |

فقط `curl_exec` هست (۱۹ مورد) که برای اتصال به API ووکامرس و باسلام
لازم است، و نوشتن فایل JSON کنار همین فایل برای نگه‌داشتن تنظیمات و صف.

## کارهایی که در نسخهٔ ۸.۶۸ انجام شد

1. **سرآیند توضیحی** بالای فایل اضافه شد که صریح می‌گوید این ابزار چیست
   و چه چیزی در آن نیست. بازبین انسانی هاست معمولاً همین را می‌خواند.
2. **خط ۲۴ کیلوبایتی CSS شکسته شد.** یک خط خیلی بلند و فشرده، قوی‌ترین
   نشانهٔ «کد مبهم‌سازی‌شده» برای اسکنرهای heuristic است. حالا بلندترین
   خط حدود ۲.۹ کیلوبایت است.
3. **نام فایل** به `scraper4.php` تغییر کرد. بعضی اسکنرها روی نام‌های
   الگودار حساس‌اند.

## اگر باز هم حذف شد

این کارها را به ترتیب انجام دهید:

### ۱. فایل را در پوشهٔ فرعی و پشت رمز بگذارید
```
public_html/
  panel/
    scraper4.php
    .htaccess      ← فایل زیر
```

`.htaccess` داخل همان پوشه:
```apache
# دسترسی فقط با رمز
AuthType Basic
AuthName "Panel"
AuthUserFile /home/USERNAME/.htpasswd
Require valid-user

# فایل‌های داده هرگز از طریق وب خوانده نشوند
<FilesMatch "\.(json|lock)$">
    Require all denied
</FilesMatch>
```
`.htpasswd` را از بخش «Directory Privacy» سی‌پنل بسازید.

> 🔴 مهم: بدون این، فایل `connections.json` که **توکن باسلام و کلید
> ووکامرس** در آن است، از طریق مرورگر قابل خواندن است.

### ۲. از هاست بخواهید فایل را در فهرست سفید بگذارد
متن پیشنهادی برای تیکت:

> سلام. فایل `scraper4.php` در هاست من یک ابزار داخلی برای همگام‌سازی
> محصولات فروشگاهم با ووکامرس و باسلام است که خودم سفارشی‌سازی کرده‌ام.
> اسکنر امنیتی آن را به اشتباه مخرب تشخیص می‌دهد و حذف می‌کند.
> این فایل هیچ کد رمزگذاری‌شده یا مبهم‌سازی‌شده‌ای ندارد و از توابع
> eval، shell_exec، system، passthru، proc_open و base64_decode
> استفاده نمی‌کند. لطفاً آن را در فهرست سفید (whitelist) قرار دهید.
> در صورت نیاز، توضیحات کامل در سرآیند خود فایل نوشته شده است.

### ۳. اگر هاست همکاری نکرد
هاست را عوض کنید یا روی یک VPS ارزان بگذارید. اسکنری که فایل سالم را
بدون اطلاع حذف می‌کند، دیر یا زود دادهٔ شما را هم از بین می‌برد.

## پشتیبان بگیرید

فایل‌های زیر داده‌های شما هستند و اگر هاست فایل را حذف کند باید
دوباره بسازید. قبل از هر کاری یک نسخه از آن‌ها را دانلود کنید:

- `profiles.json` — پروفایل‌ها و محصولات
- `connections.json` — کلیدها و تنظیمات
- `category_learning.json` — آموخته‌های دسته‌بندی
- `remote_map.json` — نگاشت شناسهٔ محصولات مقصد
- `autoreply_rules.json` — قواعد پاسخ خودکار
