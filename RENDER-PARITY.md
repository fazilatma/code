# برنامهٔ وفاداری کامل Scraper 4 PHP → Render TypeScript

مبنای بازبینی: `scraper4.php` نسخه 9.80، کل فایل ۳۶٬۲۰۰ خطی — نه فقط منوی همبرگری.

> اصلاح گزارش قبلی: عدد ۵۷ فقط inventory عملیات اصلی منوی همبرگری بود و به معنی برابری کل برنامه نبود. ممیزی خودکار جدید در `scripts/parity-audit.mjs` تمام شرط‌های GET، actionهای POST، Routeهای TypeScript و actionهای رابط را استخراج می‌کند. خروجی آخر در `parity-audit.json` ذخیره می‌شود.

آخرین ممیزی خام نسخه 1.8.0: ۱۵۰ شرط GET و ۲۸ action POST در PHP، در برابر ۱۰۳ Route و ۷۲ action رابط در TypeScript. بخشی از GETهای PHP پارامتر هستند، بااین‌حال اختلاف نشان می‌دهد هنوز سازگاری کامل حاصل نشده است.

## وضعیت فعلی

| زیرسامانه | وضعیت | توضیح |
|---|---|---|
| پروفایل‌ها، سلکتورها، صفحه‌بندی | عملیاتی | PostgreSQL + انتخاب‌گر بصری |
| استخراج فهرست و جزئیات | عملیاتی | Cheerio، گالری، توضیحات، SKU، وزن و موجودی |
| صف و نگهبان | عملیاتی | PostgreSQL SKIP LOCKED، توقف و reaper |
| ووکامرس | عملیاتی پایه | ثبت رابطی، تست، ایجاد/آپدیت، دسته، تصویر و شناسه پایدار |
| باسلام | عملیاتی پایه | ثبت رابطی، تست، ایجاد/آپدیت، تنظیمات وزن/موجودی/غرفه |
| خزانه کلیدها | عملیاتی | AES-256-GCM، مشتق از ADMIN_TOKEN |
| تنظیمات کامل PHP | عملیاتی | import/export فرمت settings_*.json |
| بکاپ Render | عملیاتی | پروفایل، محصول، state و اتصالات |
| AI تک ارائه‌دهنده | عملیاتی پایه | OpenAI-compatible test و ذخیره امن |
| اعلان‌ها | عملیاتی | ارسال آزمایشی بله، روبیکا و Webhook از خزانه |
| چند ارائه‌دهنده AI، تست مدل، کاندید و مستر | عملیاتی | تست موازی، candidate filter، رأی و leaderboard پایدار |
| چند غرفه باسلام و تعدیل قیمت | عملیاتی | remote ID مستقل هر غرفه و درصد قیمت |
| اتصال غیرمستقیم/DoH/Proxy/Worker | عملیاتی برای AI و مبدأ | Direct، DoH، IP، HTTP/HTTPS Proxy، Worker، fallback و کنترل پروفایل |
| محصولات حذف‌شده از مبدأ | عملیاتی | تشخیص پس از استخراج موفق، preview و اعمال مقصد |
| مغایرت‌گیری مقصد و remote-map rebuild | عملیاتی | تطبیق ID/SKU/عنوان و بازسازی نگاشت |
| ویرایش گروهی مقصد | عملیاتی | preview/apply عنوان، قیمت و موجودی با تأیید صریح |
| عکس‌دارکردن ووکامرس | عملیاتی | تشخیص تصویر خالی، preview و ثبت تصویر مبدأ |
| یادگیری دسته‌بندی | عملیاتی | ۱ تا ۵ کلمه، رأی پرتکرار، import/export و اتصال به ارسال باسلام |
| پاسخ خودکار گفتگوها | عملیاتی | قواعد/AI، محدودیت زمانی، preview/apply، لاگ و Cron |
| گزارش شبانه | عملیاتی | snapshot، جدید/حذف/تغییر قیمت و ارسال پیام‌رسان |
| سفارش‌ها و گفتگوهای باسلام | عملیاتی | استعلام vendor-parcels و chats از منو |
| نسخه کد و نصب‌کننده | متفاوت در Render | به Deploy/rollback خود Render نگاشت می‌شود |

## ترتیب تحویل انتخاب‌شده

1. **فاز ۱ — زیرساخت و مهاجرت:** منو، Vault، PostgreSQL، صف، پروفایل، استخراج، انتقال settings و اتصال‌های کامل.
2. **فاز ۲ — ارتباطات (انجام شد):** AI چندارائه‌دهنده، رأی مدل، اعلان‌های بله/روبیکا، چندغرفه باسلام و مسیرهای شبکه.
3. **فاز ۳ — نگهداری مقصد (انجام شد):** retirement، recon، bulk editor، photo-fix و remote-map rebuild.
4. **فاز ۴ — اتوماسیون (انجام شد):** یادگیری دسته، پاسخ خودکار، گزارش شبانه، سفارش‌ها و گفتگوها.
5. **فاز ۵ — تطبیق منوی همبرگری (انجام شد):** inventory ۵۷ عملیات اصلی، خودآزمون runtime و تست build.
6. **فاز ۶ — ممیزی کل فایل (در حال انجام):** همه GET/POSTها، import/export، مدیریت صف‌ها، دسته‌ها و APIهای مدیریتی مقصد.
7. **فاز ۷ — سازگاری پیشرفته (در حال انجام):** SSE، گزارش پسوند، duplicate ووکامرس، rejected/fix-category باسلام و پیشنهاد گالری انجام شده؛ ابزارهای AI محصول و همه گزارش‌های ریز هنوز بازند.
8. **فاز ۸ — آزمون fixture (آغاز شد):** fixture HTML برای قیمت فارسی، URL نسبی و کلید پایدار اضافه شده؛ مقایسه با حساب آزمایشی واقعی هنوز نیازمند Credential تست جداگانه است.
9. **فاز ۹ — سازگاری API قدیمی (در حال انجام):** مسیر احراز‌شده `/scraper4.php` برای کلاینت‌های query/form قدیمی اضافه شده و عملیات ناشناخته به‌صراحت 501 می‌گیرند.
10. **فاز ۱۰ — مرکز هوش مصنوعی (انجام شد):** پنج تب مشابه PHP، import/toggle/select ارائه‌دهنده، تست پس‌زمینه، توقف/وضعیت، مدل‌ها، کاندید/مستر/رأی و عیب‌یابی شبکه.
11. **فاز ۱۱ — تثبیت سلکتورها (انجام شد):** workbench یکپارچه، پیشنهاد خودکار، URL نمونه محصول، تست جزئیات، انتخاب بصری جداگانه، پیشنهاد گالری و تست‌های regression.
12. **فاز ۱۲ — ابزار AI محصول (انجام شد):** پیشنهاد دسته از دسته‌های زنده باسلام، prefilter واژگانی، preview/apply گروهی محصولات ردشده و پیشنهاد دسته محصولات محلی.
13. **فاز ۱۳ — اعلان‌ها و یادآوری‌ها (انجام شد):** snapshot رویدادها، سفارش/گفتگو/محصول/قیمت/موجودی، پینگ Cron، یادآوری بی‌جواب، ارسال انتخابی و محافظ first-run seed.
14. **فاز ۱۴ — تحلیل حرفه‌ای مدل‌ها (انجام شد):** جدول تمام‌صفحه زنده، آمار، فیلتر، جست‌وجو، مرتب‌سازی، CSV، خلاصه ارائه‌دهنده، پاسخ پیام/دسته و رأی مستقیم کاندیدها.
15. **فاز ۱۵ — بکاپ GitHub (انجام شد):** تنظیم از رابط، رمزنگاری AES-GCM، آپلود/list/download/restore، زمان‌بندی و نگاشت Legacy.
16. **فاز ۱۶ — شبکه مبدأ (انجام شد):** Direct/DoH/IP/HTTP Proxy/Worker، fallback، timeout، عیب‌یابی و تیک مستقل هر پروفایل.
17. **فاز ۱۷ — محصولات متغیر (انجام شد):** استخراج ساختاری Woo، گروه/تنوع/SKU/قیمت/موجودی/تصویر، همگام‌سازی variable و انتقال CSV/XLSX.
18. **فاز ۱۸ — صفحه‌بندی کامل (انجام شد):** query page/custom، path/full pattern، next selector، انتخاب بصری دکمه بعد، loop guard و تست URL.
19. **فاز ۱۹ — دفتر تغییرات و گزارش استخراج (انجام شد):** added/updated/unchanged/removed تراکنشی، diff فیلدی، گزارش Job، گزارش‌های اخیر و بکاپ تاریخچه.
20. **فاز ۲۰ — گزارش حرفه‌ای (انجام شد):** تب گزارش‌ها، فیلتر زمانی/پروفایل، CSV، digest مبتنی بر ledger، خلاصه پروفایل و retention خودکار.
21. **فاز ۲۱ — صف مقاوم (انجام شد):** dedup با advisory lock، pause/resume، retry نمایی محدود، available_at، heartbeat جزئیات و شمارنده تلاش.
22. **فاز ۲۲ — محصولات انتخابی (انجام شد):** payload پایدار Job، ادغام کلیدها، ارسال انتخابی Woo/Basalam، ویرایش محلی، بایگانی و کنترل کارت‌ها.
23. **فاز ۲۳ — استودیوی بصری سلکتورها (انجام شد):** viewport واکنش‌گرا، تاریخچه، auto-next، ناوبری DOM/لینک، selector پایدار، preview زنده و کنترل تک‌فیلد.
24. **فاز ۲۴ — رفع باگ و نظم سلکتورها (انجام شد):** جداسازی sub-tabها از AI، auto-next شرطی، navigator فیلدها، required/optional progress، فیلتر جزئیات و پاک‌سازی گروهی.
25. **فاز ۲۵ — اصلاح count و جریان جزئیات (انجام شد):** selector عمومی به‌جای unique، شمارش container-scoped، تست DOM واقعی، header دو ردیفی و انتقال خودکار به صفحه محصول نمونه.

## نتیجهٔ تطبیق منوی همبرگری (نه کل برنامه)

- ۵۷ عملیات منوی PHP شناسه یکتا و وضعیت قابل ماشین‌خواندن دارند (`render-src/parity.ts`).
- عملیات مخرب فقط با `APPLY` و ارسال گزارش فقط با `SEND` اجرا می‌شوند.
- `/api/selftest` اتصال دیتابیس، جداول، خزانه و تعداد قابلیت‌ها را در محیط واقعی بررسی می‌کند.
- `/api/parity` گزارش کامل قابلیت‌ها را برای رابط برمی‌گرداند.
- دو مورد عمداً **account-dependent** هستند: اتصال غیرمستقیم همه endpointهای باسلام و مسیر شبکه مبدأ. عملکرد آن‌ها به نوع Proxy و مجوز API حساب بستگی دارد.
- نصب نسخه کد و rollback به امکانات Deploy خود Render نگاشت شده و اجرای فایل‌محور PHP را تقلید نمی‌کند.

هیچ گزینه‌ای که فقط ظاهر داشته باشد «کامل» علامت نخورده است؛ موارد account-dependent در API و رابط صریحاً مشخص‌اند.
